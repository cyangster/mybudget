/** Default unspent cushion when judging free cash. Overridden per user in settings. */
export const DEFAULT_MONTHLY_SPEND_BUFFER = 200

/** @deprecated Prefer settings.monthlySpendBuffer from useBudget */
export const MONTHLY_SPEND_BUFFER = DEFAULT_MONTHLY_SPEND_BUFFER
